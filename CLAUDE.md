# AgentHub 项目约定

## 当前阶段：不考虑兼容性和历史数据迁移

项目尚未上线，直到本段由用户手动删除前，所有实现默认遵守：

- 不为旧数据库结构编写迁移兼容逻辑。
- 不为历史数据做 backfill / legacy fallback。
- 数据模型变更以当前目标结构为准，必要时允许本地重建数据库。
- 不为了兼容旧 API 或旧字段保留额外分支；除非用户明确要求。
- 数据库不使用外键约束；表之间的关联关系由应用层逻辑和索引维护。

## 前端样式核心要求：Apple 风格

前端界面默认遵守 Apple 风格视觉语言。保持整体风格气质即可，允许实现细节灵活调整，但不要偏离核心识别度。

### Style Signals

- Apple
- 极简
- 高端
- 科技
- 产品
- 留白

### Prefer

- 使用大量留白，让内容呼吸。
- 使用 Apple 灰 `#f5f5f7` 作为背景。
- 使用 Apple 蓝 `#0071e3` 作为强调色。
- 使用精致的圆角，例如 `rounded-xl` 或 `rounded-2xl`。

### Avoid

- 禁止使用过多颜色。
- 禁止使用渐变背景。
- 禁止使用重阴影。

### Output Guidance

- 先保证整体风格识别度，再优化细节。
- 避免过度炫技，保持可读性与可维护性。
